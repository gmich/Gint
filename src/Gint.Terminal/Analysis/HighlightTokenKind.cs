namespace Gint.Terminal.Analysis
{
    internal enum HighlightTokenKind
    {
        Unknown,
        Whitespace,
        Option,
        Keyword,
        EOF,
        Pipe,
        DoubleQuotes,
        SingleQuote
    }
}
